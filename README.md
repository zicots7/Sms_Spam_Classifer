# Email/Sms Spam Classifier
### Required files are mentioned "Requirement.txt" file
### Make the requirement file runs on virtual enviornment using "pip  install -t requirement.txt " command.
### the following model.pkl file is the trained ml model .
### Preprocessor.pkl is used to sanitized the raw input from user.
### CI/CD pipeline would be triggered when commit to main branch of repository.